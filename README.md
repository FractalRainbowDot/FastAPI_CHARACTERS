# RPG Battle API

Это API для текстовой RPG, реализованное на FastAPI. Оно предоставляет эндпоинты для управления персонажами, NPC и проведения боев между ними.

## 🚀 Быстрый старт с Docker

1. **Склонируйте репозиторий:**
   ```bash
   git clone <URL вашего репозитория>
   cd PY_progects
   ```

2. **Соберите и запустите Docker-контейнер:**
   ```bash
   docker build -t rpg-battle-api .
   docker run -p 8000:8000 rpg-battle-api
   ```

3. **Откройте документацию API:**
   Перейдите в браузере по адресу [http://localhost:8000/docs](http://localhost:8000/docs).

## 🛠️ Локальная установка

1. **Склонируйте репозиторий:**
   ```bash
   git clone <URL вашего репозитория>
   cd PY_progects
   ```

2. **Создайте и активируйте виртуальное окружение:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Для Windows: venv\Scripts\activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запустите приложение:**
   ```bash
   uvicorn src.app.main:app --reload
   ```

5. **Откройте документацию API:**
   Перейдите в браузере по адресу [http://localhost:8000/docs](http://localhost:8000/docs).

## ⚙️ Структура проекта

- `src/app/main.py`: Точка входа в приложение FastAPI.
- `src/app/routers/`: Роутеры для различных эндпоинтов (база данных, бои).
- `src/app/schemas/`: Pydantic-схемы и модели SQLAlchemy.
- `src/app/database/`: Логика для работы с базами данных SQLite.
- `src/app/battle/`: Бизнес-логика для проведения боев.
- `Dockerfile`: Файл для сборки Docker-образа.
- `.github/workflows/deploy.yml`: Workflow для CI/CD.
